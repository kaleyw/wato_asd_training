costmap_node:
  ros__parameters:
    resolution: 0.1        # meters per cell
    width: 300             # cells (300 * 0.1 = 30 m)
    height: 300
    inflation_radius: 1.0  # meters
    max_cost: 100
